# Backend URL (used server-side by Next.js rewrites)
# For production: use localhost since frontend and backend run on same server
BACKEND_URL=http://127.0.0.1:8080

# Observability
LOKI_URL=http://loki:3100
PROMETHEUS_PUSHGATEWAY_URL=http://pushgateway:9091
LOG_LEVEL=info

# Datadog RUM (only used in production)
NEXT_PUBLIC_DD_APPLICATION_ID=
NEXT_PUBLIC_DD_CLIENT_TOKEN=
NEXT_PUBLIC_DD_SITE=us5.datadoghq.com
NEXT_PUBLIC_DD_ENV=prod
NEXT_PUBLIC_DD_VERSION=0.0.1
